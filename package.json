{
  "name": "blockchain",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "compile": "tsc",
    "start": "tsc && node dist/index.js",
    "format": "npx prettier -w $(git ls-files '*.ts' '*.json')"
  },
  "devDependencies": {
    "ts-loader": "^9.5.2",
    "typescript": "^5.8.2"
  }
}
